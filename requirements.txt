# ============================================================
# Cerdas — Week 1 requirements (data prep + EDA + baseline)
# ============================================================
# Install: pip install -r requirements.txt
#
# We add more deps in later weeks (transformers, chromadb, openai, streamlit).
# Keep Week 1 minimal to avoid install pain.

# Core data stack
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# EDA visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter for notebooks
jupyter>=1.0.0
ipykernel>=6.25.0

# Optional but useful
tqdm>=4.65.0      # progress bars
python-dotenv>=1.0.0  # env vars
